[project]
name = "ve-measure"
version = "0.1.0"
description = "Booking and revenue attribution for Velo Engage"
requires-python = ">=3.11"
dependencies = [
    "nats-py>=2.15.0",
    "psycopg2-binary>=2.9.12",
]

[build-system]
requires = ["uv_build>=0.11.23,<0.12.0"]
build-backend = "uv_build"
