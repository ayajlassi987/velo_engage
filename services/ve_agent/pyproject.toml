[project]
name = "ve-agent"
version = "0.1.0"
description = "STUB — LangGraph structural placeholder for the VE Agent module. Not deployed; Temporal (ve_orchestrator) is the live execution engine."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "langgraph>=0.2.0",
]

[build-system]
requires = ["uv_build>=0.11.23,<0.12.0"]
build-backend = "uv_build"
