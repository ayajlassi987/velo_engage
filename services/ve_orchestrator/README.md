# ve_orchestrator

Temporal workflow and worker for the Velo Engage daily campaign pipeline.

Run the worker locally with:

```bash
uv run --package ve-orchestrator python -m ve_orchestrator.worker
```
