DB_HOST=localhost
DB_PORT=5432
DB_NAME=velodb
DB_USER=velo
DB_PASSWORD=velo_secret

TEMPORAL_ADDRESS=localhost:7233

# Local dev only — in Docker this is set by the Dockerfile itself, since it
# always resolves to the same known path there.
DENTAL_PATHWAY_DIR=
